#!/bin/bash

#Specify how many days to keep the Unified2 logs before deleting.
DAYS=5

if [ -d /var/log/suricata ]; then

	/usr/bin/find  /var/log/suricata/ -name "unified2.*"  -mtime +$DAYS -exec rm {} \;

else 

	echo "Suricata directory doesn't exist!!!"

fi
